import { createStore } from 'vuex';
import { createApp } from 'vue'; // This is how you should import createApp in Vue 3

const store = createStore({
    // Your Vuex store options
});

export default store;
